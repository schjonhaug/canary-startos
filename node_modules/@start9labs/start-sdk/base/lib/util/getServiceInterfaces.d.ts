import { Effects } from "../Effects";
import { ServiceInterfaceFilled } from "./getServiceInterface";
export declare class GetServiceInterfaces {
    readonly effects: Effects;
    readonly opts: {
        packageId?: string;
    };
    constructor(effects: Effects, opts: {
        packageId?: string;
    });
    /**
     * Returns the service interfaces for the package. Reruns the context from which it has been called if the underlying value changes
     */
    const(): Promise<ServiceInterfaceFilled[]>;
    /**
     * Returns the service interfaces for the package. Does nothing if the value changes
     */
    once(): Promise<ServiceInterfaceFilled[]>;
    private watchGen;
    /**
     * Watches the service interfaces for the package. Returns an async iterator that yields whenever the value changes
     */
    watch(abort?: AbortSignal): AsyncGenerator<ServiceInterfaceFilled[], void, unknown>;
    /**
     * Watches the service interfaces for the package. Takes a custom callback function to run whenever the value changes
     */
    onChange(callback: (value: ServiceInterfaceFilled[] | null, error?: Error) => {
        cancel: boolean;
    } | Promise<{
        cancel: boolean;
    }>): void;
    /**
     * Watches the service interfaces for the package. Returns when the predicate is true
     */
    waitFor(pred: (value: ServiceInterfaceFilled[] | null) => boolean): Promise<ServiceInterfaceFilled[] | null>;
}
export declare function getServiceInterfaces(effects: Effects, opts: {
    packageId?: string;
}): GetServiceInterfaces;
