import type { DataUrl } from "./DataUrl";
export type CurrentDependencyInfo = {
    title: string | null;
    icon: DataUrl | null;
    versionRange: string;
} & ({
    kind: "exists";
} | {
    kind: "running";
    healthChecks: string[];
});
