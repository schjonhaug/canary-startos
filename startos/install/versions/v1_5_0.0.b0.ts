import { VersionInfo } from '@start9labs/start-sdk'

export const v_1_5_0_0_b0 = VersionInfo.of({
  version: '1.5.0:0-beta.0',
  releaseNotes: `Adds self-hosted sign-in support with generated StartOS login credentials that persist across restarts and upgrades.`,
  migrations: {},
})
