import { VersionInfo } from '@start9labs/start-sdk'

export const v_1_5_0_0_b0 = VersionInfo.of({
  version: '1.5.0:0-beta.0',
  releaseNotes: `Fixes self-hosted sign-in, sign-out, auth token recovery, and session validation; adds transaction explorer selection and dark mode; improves wallet transaction performance, sync progress, mobile layouts, and accessibility; and fixes transaction list layout, translated API errors, and recurring donation configuration.`,
  migrations: {},
})
